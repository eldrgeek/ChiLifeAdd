{
  "name": "chi-life-movement-portfolio",
  "version": "1.0.0",
  "description": "Ad portfolio for Chi Life Movement",
  "main": "index.html",
  "scripts": {
    "dev": "live-server --port=5173 --open=/ --watch=. --wait=200",
    "start": "live-server --port=8000 --no-browser",
    "build": "mkdir -p dist && cp -r *.html ads designs images dist/"
  },
  "keywords": [
    "chi-life",
    "tai-chi",
    "ads",
    "portfolio"
  ],
  "author": "Chi Life Movement",
  "license": "ISC",
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}
